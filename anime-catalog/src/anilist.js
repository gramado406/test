// Simple AniList helper (stub)
// Replace with real GraphQL queries to AniList if needed

async function search(query, limit = 10) {
  // Placeholder: return an empty list. Implement real fetch to AniList GraphQL API.
  return [];
}

module.exports = { search };
